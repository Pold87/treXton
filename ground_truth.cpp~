#include <iostream>
#include <string>

int main(int argc, char *argv[])
{
  // int num_pics = argv[1];
  int num_pics = 500;
  char background_map[] = "../draug/img/bestnewmat.png"

  FILE* fp;
  fp = fopen("sift_targets.csv")

  for (i = 0; i < N; i++) {
    
  }

  

  return 0;
}
